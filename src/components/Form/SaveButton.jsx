import React from "react";
import { Button } from "antd";

const SaveButton = () => {
  return (
    <Button type="primary" htmlType="submit" block className="content-button">
      Save
    </Button>
  );
};

export default SaveButton;
